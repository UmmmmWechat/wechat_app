export default {
    /**
     * 查询是否是新的guide
     * @param {string} id 
     * @param {Function} resolve 
     * @param {Function} reject 
     */
    queryIfNew (id, resolve, reject) {
        resolve(true);
    }
}